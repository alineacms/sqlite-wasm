{
  "name": "@alinea/sqlite-wasm",
  "version": "0.1.6",
  "license": "MIT",
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": "./dist/index.js",
  "scripts": {
    "build": "make build-dist",
    "build:ts": "tsc && cp src/sqlite3-emscripten.d.ts dist/sqlite3-emscripten.d.ts",
    "bundle": "esbuild --minify --format=esm --external:@alinea/iso --bundle src/index.ts --outfile=dist/index.js",
    "snip": "node script/snip.js",
    "clean": "make clean",
    "test": "node test.js"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@alinea/iso": "^0.1.6"
  },
  "devDependencies": {
    "@types/emscripten": "^1.39.6",
    "@types/sql.js": "^1.4.3",
    "esbuild": "^0.14.43",
    "typescript": "^4.6.2"
  }
}
